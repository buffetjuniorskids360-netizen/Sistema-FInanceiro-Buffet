🚀 Prompt Completo para Configuração no Replit - Buffet Juniors Kids

Act as a **Replit Deployment Specialist** with expertise in full-stack JavaScript applications. You have been provided with the complete documentation for the "Buffet Juniors Kids - Sistema de Gestão Financeira" project and must configure it to run perfectly on Replit. Follow these instructions EXACTLY:

---

## 📌 INFORMAÇÕES DO PROJETO (JÁ FORNECIDAS)

### Visão Geral
- **Nome**: Buffet Juniors Kids - Sistema de Gestão Financeira
- **Descrição**: Sistema completo de gestão para buffet infantil que automatiza controle de eventos, clientes, pagamentos e finanças
- **Stack Técnica**:
  - Backend: Node.js 18+, Express, Prisma ORM
  - Frontend: React 18+, TypeScript 5+, Vite
  - Banco de Dados: PostgreSQL 14+
  - Autenticação: JWT
  - Upload de Arquivos: Multer

### Estrutura do Projeto

Sistema Financeiro/
├── api/
├── controllers/
├── frontend/ (React app)
├── middlewares/
├── models/
├── prisma/ (schema.prisma)
├── public/
│ └── uploads/ (para arquivos)
├── services/
├── utils/
├── .env
├── .gitignore
├── package.json
├── server.js
└── README.md

---

## 🛠️ INSTRUÇÕES ESPECÍFICAS PARA CONFIGURAÇÃO NO REPLIT

### 1. CRIE OS ARQUIVOS NECESSÁRIOS

**1.1 Crie o arquivo `replit.nix` na RAIZ do projeto com:**
```nix
{ pkgs }: {
  deps = [
    pkgs.nodejs-18_x
    pkgs.nodePackages.typescript
    pkgs.nodePackages.prisma
    pkgs.nodePackages.@prisma/client
    pkgs.postgresql
    pkgs.libpq
    pkgs.openssl
    pkgs.vite
  ];
  devSh = "export DATABASE_URL='postgresql://replit:replit@localhost:5432/buffet_juniors_kids'"
}

1.2 Crie o arquivo .replit na RAIZ com:

[run]
command = "bash start-replit.sh"

1.3 Crie o arquivo start-replit.sh na RAIZ com PERMISSÃO DE EXECUÇÃO (chmod +x start-replit.sh):

#!/bin/bash
# Configuração completa para Buffet Juniors Kids no Replit

echo "🚀 Iniciando configuração do Buffet Juniors Kids no Replit..."

# 1. Iniciar PostgreSQL
echo "🐘 Iniciando PostgreSQL..."
sudo service postgresql start
sudo -u postgres psql -c "CREATE USER replit WITH PASSWORD 'replit';" || true
sudo -u postgres psql -c "ALTER USER replit WITH SUPERUSER;" || true
sudo -u postgres psql -c "CREATE DATABASE buffet_juniors_kids OWNER replit;" || true

# 2. Verificar e criar pastas necessárias
echo "📁 Criando estrutura de diretórios..."
mkdir -p public/uploads
chmod -R 777 public/uploads

# 3. Instalar dependências do backend
echo "📦 Instalando dependências do backend..."
npm install

# 4. Instalar dependências do frontend
echo "📦 Instalando dependências do frontend..."
if [ -d "frontend" ]; then
  cd frontend
  npm install
  cd ..
else
  echo "⚠️ Diretório frontend não encontrado. Criando um novo projeto React..."
  npm create vite@latest frontend -- --template react-ts
  cd frontend
  npm install
  npm install @radix-ui/react-icons lucide-react @tanstack/react-query
  cd ..
fi

# 5. Configurar variáveis de ambiente
echo "🔑 Configurando variáveis de ambiente..."
if [ ! -f ".env" ]; then
  echo "DATABASE_URL=\"postgresql://replit:replit@localhost:5432/buffet_juniors_kids\"" > .env
  echo "PORT=3000" >> .env
  echo "NODE_ENV=development" >> .env
  echo "JWT_SECRET=\"$(node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\")\"" >> .env
  echo "UPLOAD_PATH=\"/home/runner/public/uploads\"" >> .env
  echo "MAX_FILE_SIZE=10485760" >> .env
  echo "CORS_ORIGIN=\"https://\$REPL_SLUG.\$REPL_OWNER.repl.co\"" >> .env
fi

# 6. Executar migrações do Prisma
echo "🔄 Executando migrações do Prisma..."
npx prisma generate
npx prisma migrate dev --name init

# 7. Iniciar os servidores
echo "🚀 Iniciando aplicações..."
npm install -g concurrently
concurrently "npm run backend" "cd frontend && npm run dev"

# Diagnóstico final
echo "✅ Configuração concluída!"
echo "Backend rodando em: https://\$REPL_SLUG.\$REPL_OWNER.repl.co"
echo "Frontend rodando em: https://\$REPL_SLUG.\$REPL_OWNER.repl.co"

1.4 Crie o arquivo package.json na RAIZ se não existir:

{
  "name": "buffet-juniors-kids",
  "version": "1.0.0",
  "description": "Sistema de Gestão Financeira para Buffet Infantil",
  "main": "server.js",
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "concurrently \"npm run backend\" \"cd frontend && npm run dev\"",
    "backend": "node server.js",
    "db:init": "sudo service postgresql start && psql -U replit -c 'CREATE DATABASE buffet_juniors_kids;' || true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.0",
    "prisma": "^5.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}

1.5 Crie o arquivo server.js na RAIZ se não existir:

require('dotenv').config();
const express = require('express');
const cors = require('cors');
const path = require('path');

const app = express();
const PORT = process.env.PORT || 3000;

// Configuração CORS
const corsOptions = {
  origin: process.env.CORS_ORIGIN || 'https://localhost:5173',
  credentials: true
};
app.use(cors(corsOptions));

// Middleware
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

// Rotas básicas para teste
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', message: 'Backend funcionando!' });
});

// Servir arquivos estáticos (uploads)
app.use('/uploads', express.static(path.join(__dirname, 'public', 'uploads')));

// Rota 404
app.use((req, res) => {
  res.status(404).json({ error: 'Rota não encontrada' });
});

// Iniciar servidor
app.listen(PORT, () => {
  console.log(`✅ Servidor rodando na porta ${PORT}`);
  console.log(`📌 URL: http://localhost:${PORT}`);
});

2. CONFIGURAÇÃO DE SECRETS NO REPLIT
Instrua o usuário a configurar estes SECRETS no Replit (não use .env):

DATABASE_URL = postgresql://replit:replit@localhost:5432/buffet_juniors_kids
JWT_SECRET = [VALOR_GERADO_ALEATORIO]
NODE_ENV = development
PORT = 3000
CORS_ORIGIN = https://[SEU_REPL].replit.dev
UPLOAD_PATH = /home/runner/public/uploads
MAX_FILE_SIZE = 10485760

Como gerar JWT_SECRET:

Abra o console do Replit
Execute: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
Copie o resultado e cole como valor de JWT_SECRET nos Secrets
3. CONFIGURAÇÃO ESPECÍFICA PARA UPLOAD DE ARQUIVOS
Solução para permissões no Replit:

Crie o diretório de uploads:

mkdir -p public/uploads

Defina permissões adequadas:

chmod -R 777 public/uploads

Configure UPLOAD_PATH no .env:

UPLOAD_PATH="/home/runner/public/uploads"

4. RESOLUÇÃO DE PROBLEMAS COMUNS NO REPLIT
Problema: "FATAL: Peer authentication failed for user 'replit'"

Solução: Execute no console:

sudo -u postgres psql -c "CREATE USER replit WITH PASSWORD 'replit';"
sudo -u postgres psql -c "ALTER USER replit WITH SUPERUSER;"

Problema: "EACCES: permission denied" para uploads

Solução: Execute:

chmod -R 777 public/uploads

Problema: "Error: connect ECONNREFUSED 127.0.0.1:5432"

Solução: Execute:

sudo service postgresql start

Problema: CORS errors entre frontend e backend

Solução: Configure CORS_ORIGIN nos Secrets como:

CORS_ORIGIN = https://[SEU_REPL].replit.dev

5. CHECKLIST DE VERIFICAÇÃO FINAL
Arquivo replit.nix criado corretamente
Arquivo start-replit.sh com permissão de execução
Secrets configurados no Replit
PostgreSQL iniciado automaticamente
Permissões de upload configuradas
Migrações do Prisma executadas
Backend na porta 3000 e frontend na 5173
Proxy configurado corretamente
📝 REGRAS PARA SUA RESPOSTA
NÃO EXPLIQUE - APENAS FORNEÇA OS ARQUIVOS: Você deve gerar os arquivos EXATOS que precisam ser criados
TODOS OS ARQUIVOS DEVEM SER PRONTOS PARA COPIAR/COLAR: Sem placeholders, com conteúdo funcional
INCLUA COMENTÁRIOS EXPLICATIVOS NOS ARQUIVOS: Para que o usuário entenda cada parte
DESTAQUE OS 3 PASSOS MAIS CRÍTICOS: No início da resposta, liste os 3 passos que NÃO podem ser pulados
FINALIZE COM UM RESUMO DE TROUBLESHOOTING: Os 3 problemas mais comuns e suas soluções
👉 Sua resposta deve começar com: "Configuração Replit para Buffet Juniors Kids - Sistema de Gestão Financeira está pronta. Siga ESTES 3 PASSOS CRÍTICOS primeiro:"

Configure os SECRETS no Replit (DATABASE_URL, JWT_SECRET, etc.)
Dê permissão de execução ao start-replit.sh: chmod +x start-replit.sh
Execute ./start-replit.sh para iniciar a configuração automática

Este prompt contém **todas as informações específicas do projeto** que você me enviou, já adaptadas para o ambiente do Replit. Basta você copiar este prompt completo e enviar para o agente AI no Replit - ele irá criar automaticamente todos os arquivos necessários com as configurações corretas para o seu projeto "Buffet Juniors Kids".

O prompt inclui:
- Configuração específica do PostgreSQL no Replit
- Scripts de inicialização automatizados
- Soluções para problemas comuns do Replit (permissões, CORS, etc.)
- Estrutura de diretórios completa
- Configuração de Secrets (não use .env no Replit)
- Comandos prontos para copiar/colar

É um prompt "pronto para uso" que já incorpora todas as informações do seu projeto que você compartilhou no arquivo.